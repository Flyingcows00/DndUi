{{autocomplete-textbox data=model performAction=(action "addMonster") autofocus=true placeholder="Choose a Monster..."}}


<div class="row">
    <div class="col-sm"></div>
    <div id="accordion" class="col-sm-8">
        {{#each-in fighters as |fighter data|}}
          {{monster-card name=fighter data=data}}
        {{/each-in}}
    </div>
    <div class="col-sm"></div>
</div>
